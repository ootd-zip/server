spring:
  security:
    oauth2:
      client:
        registration:
          apple:
            client-id: # input apple client id
            team-id: # input apple App ID Prefix
            key-id: # input apple Key ID
            private-key: # input apple AuthKey_*.p8 private key content
          kakao:
            client-id: # kakao rest api app key
            client-secret: # kakao client secret
          dev:
            client-id: # kakao-dev rest api app key

cloud:
  aws:
    credentials:
      access-key: # input aws access key
      secret-key: # input asw secret key
    s3:
      bucket: # input your bucket name
    region:
      static: # input your bucket region
    stack:
      auto: false

authorization:
  secret-key: # Jwt secret key
